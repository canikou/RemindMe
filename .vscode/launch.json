{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug (gdb) - CMake",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/RemindMe.exe",
      "args": [],
      "cwd": "${workspaceFolder}",
      "externalConsole": true,
      "MIMode": "gdb",
      "miDebuggerPath": "gdb",
      "preLaunchTask": "CMake: build"
    }
  ]
}
